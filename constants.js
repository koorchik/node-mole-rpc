const INTERNAL_METHODS = {
    PING: '__ping__'
};

const CLIENT_EVENTS = {
    SERVER_AVAILABLE: 'server_available',
    SERVER_UNAVAILABLE: 'server_unavailable'
};

module.exports = {
    INTERNAL_METHODS,
    CLIENT_EVENTS
};